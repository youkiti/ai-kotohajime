mkdocs==1.6.1  # サイトジェネレータ本体(現在は mkdocs-material の推移的依存だが、CI再現性のため明示固定)
mkdocs-material==9.7.6  # Material for MkDocs テーマ(検索・admonition等のプラグインを含む)
mkdocs-git-revision-date-localized-plugin==1.5.3  # 各ページ末尾に最終更新日時を自動表示(git履歴からビルド時に埋め込み。要件定義書 §4.7)
mkdocs-static-i18n==1.3.1  # 日英2言語ビルド(suffix方式。/en/ 配下に英語版を生成)
